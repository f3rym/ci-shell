// Package ui рисует консольный интерфейс ci-shell и слушает клавиши. Пакет
// ничего не печатает в стандартные потоки и не содержит доменной логики —
// воспроизведение упавшей джобы остаётся в internal/runner, internal/repo и
// internal/env, интерфейс только даёт им лицо (09-UI-SPEC.md).
package ui

import (
	"fmt"
	"time"

	"charm.land/lipgloss/v2"

	"github.com/f3rym/ci-shell/internal/textwidth"
)

// Сетка в целых ячейках терминала (09-UI-SPEC.md, «Сетка и раскладка»).
// Отступов в px не существует — вся геометрия здесь в ячейках.
const (
	// OuterMargin — отступ от левого/правого края экрана до первой рамки.
	OuterMargin = 1
	// PanelGap — горизонтальный зазор между соседними панелями.
	PanelGap = 2
	// RowIconGap — зазор между символом статуса и текстом строки списка.
	RowIconGap = 1
	// TreeIndent — отступ на уровень вложенности дерева групп и проектов.
	// Объявлена здесь, но в этой фазе не задействована: её потребитель —
	// дерево групп Фазы 10, и заводить вторую сетку там было бы ошибкой.
	TreeIndent = 2
)

// Жёсткий пол экрана (09-UI-SPEC.md, «Минимальные размеры экрана и
// раскладка по ширине»). Собственные минимумы и доля панелей шагов и
// окружения экрана джобы существовали до плана 13-02, где отрисовка
// переезжает на ленту, — и план 13-02 их убрал: лишняя ширина при росте
// окна уходит в сетку ленты (ColumnMin/ColumnsVisibleMax ниже), а не во
// второй набор минимумов и долей.
const (
	// MinWidth — жёсткий пол экрана по ширине.
	MinWidth = 80
	// MinHeight — жёсткий пол экрана по высоте.
	MinHeight = 24
)

// Замкнутый набор символов состояния (09-UI-SPEC.md, «Символы состояния»).
// Все девять шириной ровно в одну ячейку — эмодзи ломают выравнивание в
// половине терминалов (REQUIREMENTS.md → Out of Scope) и в этот набор не
// допускаются. Символ предупреждения из internal/render/banner.go (⚠) сюда
// не переезжает — вместо него в предупреждающем стиле используется
// текстовое «!».
const (
	GlyphOK     = "✓"
	GlyphFail   = "✗"
	GlyphActive = "●"
	GlyphIdle   = "○"
	GlyphHint   = "▸"
	GlyphCursor = "◀"
	GlyphUp     = "↑"
	GlyphDown   = "↓"
	GlyphEnter  = "⏎"
)

// ManualNote — подпись рядом со статусом ручного запуска, названная
// контрактом.
const ManualNote = "вручную"

// GlyphCollapsed, GlyphExpanded — символы свёрнутой и раскрытой ветки дерева
// групп и проектов (Фаза 10, BROW-01). Контракт 09-UI-SPEC.md объявил набор
// символов замкнутым и одновременно передал эту семантику Фазе 10 — набор
// пополняется ровно этими двумя символами и ровно здесь, в единственной
// таблице символов темы, а не по месту отрисовки.
const (
	// GlyphCollapsed — та же стрелка вправо, что уже маркирует строку
	// подсказки (GlyphHint).
	GlyphCollapsed = GlyphHint
	GlyphExpanded  = "▾"
)

// GlyphRight, GlyphLeft — стрелки закона ленты колонок (Фаза 13,
// NAV-01…NAV-02). Контракт 09-UI-SPEC.md объявил набор символов замкнутым и
// одновременно передал семантику навигации этой фазе — тем же приёмом, каким
// Фаза 10 добавила GlyphCollapsed/GlyphExpanded выше: набор пополняется ровно
// этими двумя символами и ровно здесь, в единственной таблице символов темы,
// а не по месту отрисовки.
const (
	GlyphRight = "→"
	GlyphLeft  = "←"
)

// Сетка ленты колонок (Фаза 13, NAV-01…NAV-04): единая сетка на ВСЕ
// колонки. Прежние минимумы и доли отдельных экранов — свои у экрана
// дерева и свои у экрана джобы — существовали до плана 13-02, где
// отрисовка переезжает на ленту, — и план 13-02 их убрал вместе с
// отрисовкой по отдельным экранам: второго набора минимумов и долей в
// проекте больше не заводится, ни для одной колонки. Прежних четырёх
// наборов (двух у экрана дерева, двух у экрана джобы) в проекте больше нет,
// и заводить второй набор — той же дефект, который эта фаза устраняет.
//
// ColumnMin посчитан так, чтобы при поле 80 (78 ячеек доступно за вычетом
// отступов от краёв) помещались ровно две колонки, а третья появлялась
// только при более широком окне:
//
//	2*ColumnMin + PanelGap    = 2*25 + 2 = 52 <= 78 (две помещаются)
//	3*ColumnMin + 2*PanelGap  = 3*25 + 4 = 79 >  78 (три — уже нет)
const (
	// ColumnMin — минимальная ширина одной колонки ленты в ячейках.
	ColumnMin = 25
	// ColumnsVisibleMax — сколько колонок видно одновременно самое большее
	// (idea-0.3.1 §3: «видимых всегда две-три»).
	ColumnsVisibleMax = 3
)

// StatusFailed — статус упавшей джобы тем же словом, что отдаёт API. Слово
// статуса в проекте не переводится (см. StatusGlyph выше); сравнение с ним
// встречается теперь в двух местах (панель лога решает, тянуть ли хвост;
// список джоб решает, куда поставить курсор) — разъехавшиеся строковые
// литералы были бы тем же дефектом, который проект уже устранял выносом
// общей функции.
const StatusFailed = "failed"

// Пары значений палитры для тёмного и светлого фона — ровно из таблицы
// «Цвет» контракта (09-UI-SPEC.md, все десять значений дословно). Значение
// цвета в этом файле не применяется ни разу — константы только передаются в
// единственную функцию применения цвета ((Caps) style, internal/ui/color.go)
// ниже, в NewTheme. Это инвариант, а не стилистика: применить цвет в обход
// той функции значило бы завести вторую точку решения, и монохром перестал
// бы выключать его разом для всех ролей (T-12-05).
const (
	accentLight = "#0087AF"
	accentDark  = "#5FD7FF"

	mutedLight = "#6C6C6C"
	mutedDark  = "#808080"

	dangerLight = "#AF0000"
	dangerDark  = "#FF5F5F"

	warningLight = "#875F00"
	warningDark  = "#D7AF00"

	borderLight = "#BCBCBC"
	borderDark  = "#444444"
)

// Theme — стили по ролям, ровно по таблице «Цвет» контракта.
type Theme struct {
	// Text — обычный текст: собственного цвета не задаёт, наследуется от
	// терминала.
	Text lipgloss.Style
	// Muted — приглушённые метаданные (время, `<скрыто>`, заголовки панелей).
	Muted lipgloss.Style
	// Accent — курсор/выбор, буква клавиши в подсказке, текущий пайплайн.
	Accent lipgloss.Style
	// Danger — единственное законное место для красного: реально упавшее.
	Danger lipgloss.Style
	// Warning — баннер допущений, предупреждения (не ошибки).
	Warning lipgloss.Style
	// Border — границы панелей Lip Gloss.
	Border lipgloss.Style
	// Selected — текущая строка списка: инверсия видео, а не цвет.
	Selected lipgloss.Style
	// ScreenTitle — заголовок экрана (bold, Sentence case).
	ScreenTitle lipgloss.Style
	// PanelTitle — заголовок панели/колонки (bold + muted, ВЕРХНИЙ РЕГИСТР
	// в вызывающем коде).
	PanelTitle lipgloss.Style
	// KeyCap — клавиша в строке клавиш (bold + accent).
	KeyCap lipgloss.Style
	// KeyDesc — описание клавиши (normal + muted).
	KeyDesc lipgloss.Style
	// HintText — текст строки подсказки (bold).
	HintText lipgloss.Style
}

// NewTheme собирает Theme для возможностей терминала c (Фаза 12, POL-01):
// профиль цвета и признак тёмного фона, а не голый bool фона, как раньше —
// возможности решают ещё и вопрос «применять ли цвет вообще» (см.
// internal/ui/color.go, DetectCaps).
//
// Каждая цветная роль — одна строка литерала структуры вида «роль:
// применение цвета к паре констант, затем начертания» — значение цвета
// здесь не встречается ни разу, только константа передаётся единственной
// функции применения ((Caps) style). Обязательные начертания — по
// монохромной колонке контракта, потому что без цвета остаются только они:
// роль отказа жирная, роль приглушённого тусклая, роль предупреждения
// подчёркнутая, роль выделенной строки — инверсия видео. Курсив не
// применяется ни к одной роли — контракт прямо запрещает его как
// непредсказуемое в Windows-эмуляторах терминала (09-UI-SPEC.md,
// «Типографика»).
func NewTheme(c Caps) Theme {
	return Theme{
		// Text — обычного цвета не задаёт, наследуется от терминала.
		Text: lipgloss.NewStyle(),
		// Роль «успех» собственного цвета не имеет намеренно: GlyphOK
		// рисуется обычным текстовым стилем (Text) — осознанное отличие от
		// типичных TUI, где успех зелёный (09-UI-SPEC.md, правило 3
		// палитры).
		Muted:   c.style(mutedLight, mutedDark).Faint(true),
		Accent:  c.style(accentLight, accentDark),
		Danger:  c.style(dangerLight, dangerDark).Bold(true),
		Warning: c.style(warningLight, warningDark).Underline(true),
		Border:  c.style(borderLight, borderDark),
		// Selected — инверсия видео, не конкретный цвет: инверсия
		// использует собственные цвета терминала и потому читается на
		// любом фоне и в монохроме без нашего выбора конкретного hex.
		Selected:    lipgloss.NewStyle().Reverse(true),
		ScreenTitle: lipgloss.NewStyle().Bold(true),
		PanelTitle:  c.style(mutedLight, mutedDark).Bold(true).Faint(true),
		KeyCap:      c.style(accentLight, accentDark).Bold(true),
		KeyDesc:     c.style(mutedLight, mutedDark).Faint(true),
		HintText:    lipgloss.NewStyle().Bold(true),
	}
}

// StatusGlyph возвращает символ статуса status ровно по таблице «Символы
// состояния» контракта. running возвращает пустую строку — его место
// занимает спиннер, единственное анимированное состояние; отрисовку кадра
// делает вызывающий код экрана.
func StatusGlyph(status string) string {
	switch status {
	case "created", "pending", "manual", "skipped":
		return GlyphIdle
	case "running":
		return ""
	case "success":
		return GlyphOK
	case "failed":
		return GlyphFail
	case "canceled":
		return GlyphFail
	default:
		return GlyphIdle
	}
}

// StatusStyle возвращает стиль символа статуса status. Правило
// доступности: символ первичен, цвет — усиление, состояние читается без
// цвета. Упавший и отменённый несут один и тот же символ (GlyphFail,
// StatusGlyph выше) — различает их именно начертание: роль отказа жирная,
// роль приглушённого тусклая, и в монохроме, где цвет не применяется вовсе,
// различие остаётся (T-12-06). Если бы отменённый получал роль отказа, без
// цвета оба статуса стали бы неразличимы — проверяемое следствие правила
// «символ первичен, цвет — усиление».
func (t Theme) StatusStyle(status string) lipgloss.Style {
	switch status {
	case "created", "pending", "manual":
		return t.Muted
	case "running":
		return t.Accent
	case "success":
		return t.Text
	case "failed":
		return t.Danger
	case "canceled":
		// Отменено человеком, а не поломка: приглушённый стиль, НЕ danger.
		return t.Muted
	case "skipped":
		// Самый тусклый вариант — тот же приглушённый стиль контракта.
		return t.Muted
	default:
		return t.Muted
	}
}

// Plain очищает строку от управляющих последовательностей терминала, не
// трогая её ширину. Нужна там, где строка попадает в кадр НЕ через меру
// ширины: заголовок панели, строка подсказки, метка тяги образа. Ссылка на
// образ приходит из .gitlab-ci.yml, а не через перенос ответа API, и своего
// санитайзера у неё нет — очистка здесь и есть её единственная (CR-06
// обзора v0.3.0). Реализация одна на проект — internal/textwidth.
func Plain(s string) string {
	return textwidth.Plain(s)
}

// Truncate обрезает s до заданной ширины width в ячейках терминала,
// добавляя многоточие «…» при обрезке. Реализация в проекте одна —
// internal/textwidth, единственная мера ширины на весь проект (Фаза 12,
// POL-02); здесь только имя, привычное пакету интерфейса. Кириллица и «…»,
// посчитанные не в графемах, уже один раз сломали `%-8s` в internal/render
// (Фаза 1), и здесь тот же класс дефекта недопустим (09-UI-SPEC.md,
// «Правило измерения ширины»).
func Truncate(s string, width int) string {
	return textwidth.Truncate(s, width)
}

// Fit приводит строку s ровно к ширине width: делегат той же единственной
// меры ширины, что и Truncate выше. Ручной fmt.Sprintf("%-Ns", ...) на
// пользовательских строках (имена джоб, ветки, сообщения) в этом пакете
// запрещён — на кириллице он считает байты, а не ячейки; выравнивание
// только через internal/textwidth.
func Fit(s string, width int) string {
	return textwidth.Fit(s, width)
}

// TooSmall возвращает единственную строку, которая рисуется вместо
// панелей, когда окно меньше пола 80×24 — отцентрированную по доступной
// ширине средствами Lip Gloss.
func TooSmall(width, height int) string {
	msg := fmt.Sprintf("терминал слишком мал: нужно минимум 80×24, сейчас %d×%d — растяните окно", width, height)
	if width <= 0 {
		return msg
	}
	return lipgloss.NewStyle().Width(width).Align(lipgloss.Center).Render(msg)
}

// Ago форматирует относительное время t для метаданных списков и шапок.
// Живёт здесь, потому что её зовут и список джоб, и шапка экрана джобы, а
// вторая копия разъехалась бы с первой.
func Ago(t time.Time) string {
	if t.IsZero() {
		return "—"
	}
	d := time.Since(t)
	switch {
	case d < time.Minute:
		return fmt.Sprintf("%d сек назад", int(d.Seconds()))
	case d < time.Hour:
		return fmt.Sprintf("%d мин назад", int(d.Minutes()))
	case d < 24*time.Hour:
		return fmt.Sprintf("%d ч назад", int(d.Hours()))
	default:
		return fmt.Sprintf("%d дн назад", int(d.Hours()/24))
	}
}
